# Dashboard screenshots

Drop the Power BI dashboard page screenshots here so the main `README.md` can embed them.
The `.pbix` itself is ~97 MB (git-ignored), so screenshots + the live Publish-to-web link
are how the dashboard is showcased in the repo.

**Suggested captures (PNG, ~1600px wide, one per report page):**

| File name to save | Report page |
|---|---|
| `01_executive_overview.png` | Executive Overview |
| `02_acquisition_funnel.png` | Acquisition & Funnel |
| `03_revenue_product_mix.png` | Revenue & Product Mix |
| `04_profitability_returns.png` | Profitability & Returns |
| `05_customer_cohort.png` | Customer & Cohort |
| `06_operations.png` | Operations & Delivery |
| `07_inventory.png` | Inventory |

In Power BI Desktop: maximize each page, hide the Filters/Visualizations panes for a clean
frame, then use `Win + Shift + S` to snip, or **File → Export → Export to PDF** and split pages.
